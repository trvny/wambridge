name: Apply cumulative PCM pacing

on:
  pull_request:
    types: [synchronize]

permissions:
  contents: write

jobs:
  apply:
    if: github.head_ref == 'fix/pace-foobar-pcm'
    runs-on: ubuntu-slim
    steps:
      - uses: actions/checkout@v7
        with:
          ref: fix/pace-foobar-pcm

      - name: Apply fix
        shell: bash
        run: |
          set -euo pipefail
          printf '\n' >> patches/cumulative-pacing.patch
          git apply --check patches/cumulative-pacing.patch
          git apply patches/cumulative-pacing.patch
          git rm -f patches/cumulative-pacing.patch
          git rm .github/workflows/apply-cumulative-pacing.yml
          git add foobar/foo_out_wam.cpp
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git commit -m "Use cumulative PCM pacing"
          git push origin HEAD:fix/pace-foobar-pcm
